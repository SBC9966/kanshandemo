# 实现边界与兼容

该文件已在v2更新；详细模块见DEVELOPMENT，逐站说明见INTERACTIONS，来源范围见SOURCEBOOK。

主地图不是水墨素材整图热区，而是可独立更新的SVG形状、交通线和DOM标签。原有24个素材保留于背景、头像与装饰。资料馆的作者字段只表示本次索引可见文本，不是账号鉴别或认证。无全文源不自动生成作者立场摘要。

进度key延续 `shan-journey-v1`，新记录内标记 `labVersion:2`。保存完成实验快照，归一化恢复时检查前置与必要字段，不宣称这是防作弊或权限边界。旧版完成记录保留；新主题/新站点执行新校验。

浏览器测试环境阻止直接导航localhost，因此使用set_content与显式测试存储适配器；适配器只在测试文件中，不进入产品。服务端HTTP由Node真实访问另测。本地浏览器的file存储、音频设备和真实模型仍需要目标环境验证。
